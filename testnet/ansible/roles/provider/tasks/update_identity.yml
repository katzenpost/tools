- name: update provider identity key
  copy: src=./identity.public.pem dest=/etc/katzenpost/identity.public.pem owner=root group=katzenpost mode=0640
  copy: src=./identity.private.pem dest=/etc/katzenpost/identity.private.pem owner=root group=katzenpost mode=0640
  notify:
    - restart katzen-provider
