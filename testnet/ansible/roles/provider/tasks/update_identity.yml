---
- name: update provider identity public key
  copy: src="{{ idPubKey }}" dest=/etc/katzenpost/identity.public.pem owner=root group=katzenpost mode=0640

- name: update provider identity private key
  copy: src="{{ idPrivKey }}" dest=/etc/katzenpost/identity.private.pem owner=root group=katzenpost mode=0640
  notify:
    - restart katzen-provider
