- name: create /etc/katzenpost
  file:
    path: latest
    state: directory

- name: update katzen-authority config
  copy: src="{{ authorityConfig }}" dest=/etc/katzenpost/nonvoting.toml owner=root group=root mode=0644
  notify:
    - restart katzen-authority
